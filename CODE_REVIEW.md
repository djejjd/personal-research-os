# 代码审查规范

## 目标与适用范围

代码审查验证已批准范围内的正确性、可恢复性、安全性和可维护性，不顺带扩大需求。所有面向 `main` 的 PR 在创建前都必须完成独立审查；作者自检、构建测试和生成代码的 Agent 自述不构成独立审查。GitHub 只能在合并前强制检查，不能在服务器端阻止 PR 被创建；因此“创建前”由提交者流程强制，合并前由分支保护和审批强制。

## 独立性的定义

审查者必须不是该变更的作者，并以独立上下文阅读任务、设计约束、`main...HEAD` 差异和测试证据。审查者可以是另一位人工开发者，或一个未参与本次实现、只获得审查任务的独立 Agent。审查者不得在给出首轮结论前修改被审查代码。

## 审查时机与门禁

1. 作者完成本地验证后，先发起独立审查，再创建 PR。
2. 审查报告必须记录在 PR 中；报告前不得将 PR 标记为可合并。创建 PR 时必须填入审查者和可定位的审查记录，模板字段不能替代真实审查。
3. 数据、权限、审批、副作用、存储、迁移、恢复、跨进程协议或安全边界变化属于高风险，至少需要一次独立对抗性审查。
4. 高严重度或中严重度问题未关闭时不得创建合并请求或合并。修复后由原审查者或另一独立审查者复审受影响差异。

## 审查清单

审查至少覆盖：

- 范围、任务和设计约束是否一致，非目标是否被意外实现；
- 层次依赖、公共接口、错误码、异常和资源生命周期；
- 外部输入、路径、SQLite/文件操作、并发、迁移、幂等、恢复和回滚；
- 秘密、个人数据、日志脱敏、网络/子进程等外部副作用；
- 正常、失败、重试、重复执行和边界条件的测试；
- 构建、格式、静态分析、测试、秘密扫描和 CI 证据；
- 注释、运行说明、变更记录和用户可见影响。

## 审查报告格式

审查者先列出问题，按严重度排序，并给出文件和行号、复现或推理、影响和修复建议。严重度如下：

| 级别 | 含义 | 合并条件 |
|---|---|---|
| 高 | 数据丢失、越权、秘密泄漏、不可恢复损坏、错误公开契约或明显崩溃 | 必须修复并复审 |
| 中 | 重要行为错误、迁移/恢复缺口、测试无法证明关键契约、违反已确认规范 | 必须修复并复审 |
| 低 | 可维护性、诊断、测试覆盖或非阻塞一致性问题 | 修复或登记有负责人和截止版本的任务 |
| 建议 | 不影响合并的改进想法 | 可采纳，不作为门禁 |

没有发现问题时，报告仍须说明审查范围、运行或核对的证据，以及未覆盖的风险。

## 关闭与追溯

每项问题必须对应代码、测试、文档或明确的后续任务。作者不得自行把独立审查标为“通过”；审查者在复审后给出结论。PR 保留审查报告、修复提交、实际命令和已知限制，作为后续故障与版本验收的证据。

治理基线合并且 CI 首次成功后，仓库管理员必须在 GitHub 服务端启用：禁止直接推送 `main`、禁止 force push、要求 PR、至少一次非作者批准及全部已启用检查。启用后回读设置并把检查名称记录到 PR；在此之前不得把分支保护描述为已生效。
