# 参与开发

## 分支与提交

常规修改从最新 `main` 创建分支，不直接修改 `main`。分支格式：

```text
<type>/<TASK-ID>-<short-name>
```

例如：`feat/TASK-V01-011-project-domain`、`chore/TASK-V01-001-build-baseline`。

提交保持单一目的，使用：

```text
<type>(<scope>): <中文摘要>
```

提交前检查暂存区；禁止提交密钥、真实个人数据、构建产物、运行数据、日志和未经明确批准的 `docs/` 文件。

## 代码要求

- 文档、注释和面向开发者的说明默认中文；标识符和稳定协议字段使用英文。
- 用命名和类型表达常规行为；注释只说明契约、原因、不变量、所有权、副作用及失败恢复。
- 外部输入、路径、数据库内容和协议数据均不可信，必须在使用前校验。
- 迁移、恢复、权限、安全和数据完整性变化必须覆盖正常与失败路径；不得吞掉错误后继续未知状态。

## 本地验证

至少执行：

```bash
git diff --check
```

工程包含 `scripts/check.sh` 时，还必须执行：

```bash
scripts/check.sh
```

在 PR 中记录实际命令、结果和无法执行的项目及原因。

## Pull Request

创建 PR 前必须完成独立代码审查，具体规则见 [CODE_REVIEW.md](CODE_REVIEW.md)。PR 至少写明：

- 任务 ID、背景、范围和明确非目标；
- 设计取舍，以及数据、权限、存储、恢复、安全和兼容性影响；
- 日志和稳定错误处理变化，或明确不适用；
- 测试命令、结果、证据和已知限制；
- 独立审查报告链接、结论和未关闭问题。

默认使用 squash merge。合并前要求所有已启用门禁通过、独立审查无未关闭高/中问题，并且审查者不是改动作者。
