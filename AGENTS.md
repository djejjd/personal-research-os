# Agent 工作约定

本文件是公开仓库中 Agent 的工程协作入口。中文是默认说明语言；代码标识符、协议字段和稳定错误码使用英文。

## 开始前

1. 阅读 [CONTRIBUTING.md](CONTRIBUTING.md) 和 [CODE_REVIEW.md](CODE_REVIEW.md)。
2. 明确稳定任务 ID、批准范围、非目标、风险、验收方式和需要的证据。没有任务 ID 的工程修改不得开始；仓库 bootstrap 例外必须在 PR 中写明。
3. 从最新 `main` 创建单一目的分支，格式为 `<type>/<TASK-ID>-<short-name>`；多文件或并行工作使用独立 worktree。
4. 检查工作区和暂存区，确认没有来源不明的重叠修改、密钥、真实个人数据或生成物。

## 实现与验证

- 不直接在 `main` 开发，不自行扩大批准范围，不以口头结论代替测试或证据。
- 公共接口、端口、迁移、恢复与非平凡函数使用 Doxygen 兼容的中文注释，说明契约、前置条件、副作用、失败和幂等语义。
- 数据、权限、存储、迁移、恢复、安全、外部副作用和协议变化必须有失败路径、稳定错误处理和独立审查。
- 运行适用的格式、静态分析、构建、测试、秘密扫描和 `git diff --check`。工程存在 `scripts/check.sh` 时，必须执行它。
- `docs/` 是本地私有设计资料，默认不得暂存或推送；需要公开某份文档时，先取得明确批准并单独整理。

## 提交与审查

1. 每个提交保持可构建或可验证，消息使用 `<type>(<scope>): <中文摘要>`。
2. 在创建 PR 前，按 [CODE_REVIEW.md](CODE_REVIEW.md) 完成独立代码审查；作者不能签署自己的独立审查。
3. PR 必须记录任务、范围、非目标、风险、验证命令及结果、未执行项、审查报告和未关闭问题。
4. 未关闭的高/中风险审查问题、失败门禁或缺失独立审查时，不得合并。
